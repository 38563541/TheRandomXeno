# RN50 + flat8 上機驗證結果

日期：2026-09-18 ～ 2026-09-20
分支：`vram/rn50-runpy-0918`（合回 `stage2-decouple` 前的最終版本）
完整數據：`logs/vram_0918_runpy.csv`

## 主結論

**RN50 + flat8 在這台 3080 Ti 上，用真實的 `run.py`，訓練峰值 7.664 GB、評估峰值 0.837 GB，兩段都跑得動。**

（訓練:300 iteration,`configs/stage2_true_hyrsm_b4_tuple.yaml`,`--grad_ckpt --ckpt_segments 8`,不 OOM,峰值全程持平無漂移。評估:同一 checkpoint,`num_test_tasks` 500 與 2000 兩次獨立跑,峰值完全一致,遠低於訓練峰值。)

---

## 需要更正的三個舊說法

### 1.「時間只多 3%」

**這句話原本講的是 c4→flat8 不同切法之間的差（Colab 實測），不是開/關 ckpt 本身的差。** 兩者是不同的問題,原文沒有區分,容易被讀成「開 ckpt 幾乎不花時間」。

開/關 ckpt 的實際代價（本機 3080 Ti,RN34,唯一有裸跑對照組的 backbone,`--profile_time` 五段拆解,n=100 中位數）：

```
純 GPU 段落（H2D + backbone + head + backward）
  裸跑  30.885 + 90.604 + 32.705 + 253.568 = 407.762 ms
  flat8 30.323 + 90.868 + 33.016 + 320.477 = 474.684 ms
  多 66.922 ms → +16.42%

含 data wait 的 wall clock
  裸跑  418.099 ms → flat8 485.081 ms → +16.02%
```

三個獨立量法互相印證：階段 3 用 `/usr/bin/time -v` 粗估的 wall clock（439.6 → 510.8 ms/iter，+16.2%）、階段 5 最初版四段拆解（+17.9%／+16.1%）、以及這次補上 H2D 之後的五段拆解（+16.42%／+16.02%）——三次獨立測量都落在 16–18% 這個範圍，不是 3%。

**只有 RN34 有裸跑對照組，RN50 裸跑就 OOM，這個百分比是從 RN34 量出來再推論適用於 RN50，不是在 RN50 上直接量到的。**

### 2.「RN50 + ckpt 是 16.8 小時 / 100k iteration」

這個數字是用 probe 的 602.7 ms/iter 算的（602.7ms × 100,000 ÷ 3600 ≈ 16.74h）。**probe 量的是 backbone + pair head 的一次 forward/backward，不含 relation 模組、資料載入、H2D 搬移這些真實訓練管線裡都有的東西。**

真實 `run.py`（`--profile_time`，n=100 中位數）量到的 RN50+flat8 是 **732.618 ms/iter**：

```
732.618 ms/iter × 100,000 ÷ 3,600,000 ≈ 20.35 小時
(732.618 − 602.7) / 602.7 ≈ +21.6%  ← probe 低估了約 21%
```

三個可以拿去規劃排程的數字（都是 `--profile_time` 中位數，100,000 iteration 換算）：

| 設定 | ms/iter | 100k iteration 估計時長 |
|---|---|---|
| RN34 裸跑 | 418.099 | ≈ 11.6 小時 |
| RN34 + flat8 | 485.081 | ≈ 13.5 小時 |
| RN50 + flat8 | 732.618 | ≈ 20.3 小時 |

### 3.「凍結之後瓶頸是 video 解碼」——舊說法待修正，不是已否證

先講清楚哪個部分被推翻、哪個部分還沒有答案：

**全微調情境下，這個說法沒有量測支持。** 階段 5 三組（RN50+flat8、RN34 裸跑、RN34+flat8）的 data wait 只佔 1.27%–2.22%，不是瓶頸，跟原文暗示的「解碼拖慢訓練」矛盾。

**凍結情境（5b，RN18 B1）量過之後，「解碼」本身也不是瓶頸——但整個「拿到能餵進 head 的特徵之前」的流程確實佔壓倒性比例，只是主要成本不在解碼：**

```
data wait（dataloader 等待，可視為解碼+prefetch）  10.993 ms   9.60%
H2D（.to(device) 搬移原始影格）                    48.650 ms  42.50%
backbone fwd（凍結 backbone，support+query 兩次）  47.914 ms  41.86%
──────────────────────────────────────────────
data + H2D + backbone fwd 合計                    107.557 ms  93.97%
head + backward+step                                5.754 ms   5.03%
```

data wait 單獨只有 9.60%，不是主要瓶頸；真正吃掉時間的是 **H2D 搬移（42.50%）**跟**凍結 backbone 的 forward 本身（41.86%）**——這兩項都不是「解碼」，是解碼之後的兩個獨立步驟。舊說法把整個問題歸因到「解碼」，範圍講窄了、也講錯了主要成本在哪。

**但這不影響離線特徵快取值不值得做的結論——反而讓理由更強，只是機制不同：** 快取的是 backbone forward 之後的特徵，data wait、H2D、backbone fwd 這三段會**同時**被消掉（快取住的是最終輸出，不需要再解碼、再搬原始影格、再跑一次凍結 backbone），合計消掉 93.97% 的 iteration 時間，只剩 head+backward+step 的 5.03%。

**H2D 縮減量**（用實際 tensor 形狀算，不是估值）：

```
原始影格：context 40 張 + target 200 張，224×224×3 float32
  = 24.084 MB + 120.422 MB = 144.507 MB / iteration

快取後的特徵：context 40×512 + target 200×512 floats（RN18, 512維）
  = 0.082 MB + 0.410 MB = 0.492 MB / iteration

144.507 / 0.492 ≈ 294 倍
```

---

## 兩個機制觀察

### ckpt 的代價 100% 落在 backward 段

```
RN34 裸跑 → flat8：
  backbone_fwd   90.604 → 90.868 ms   diff +0.264ms（雜訊量級，不變）
  H2D            30.885 → 30.323 ms   diff −0.562ms（雜訊量級，不變）
  backward+step 253.568 → 320.477 ms  diff +66.909ms（幾乎吃下全部差距）
```

跟機制完全一致：`checkpoint_sequential` 的重算發生在 `backward()` 呼叫內部，不影響初次 forward 的計算量。這同時是儀器本身正確的旁證——如果 backbone_fwd 或 H2D 也跟著漲，代表計時的段界劃錯了；它們沒有漲，說明五段拆解的邊界是準的。

### head 時間不隨特徵維度變

```
RN34（512維，裸跑/flat8）  32.705 / 33.016 ms
RN50（2048維，flat8）      32.764 ms
```

三者幾乎相同，維度差 4 倍，head 時間幾乎不變。

**這是推論，不是單一量測**（跨 run、跨 backbone、跨 config，三個變因同時動，沒有做過單一變因對照）：加上 5b（B1，無 relation 模組）head 只有 2.893ms，B4（有 relation 模組，上面三組）head 是 32.7–33.0ms，這約 30ms 的落差**幾乎全部來自 relation 模組**（tuple 展開 + 配對距離），跟 backbone 特徵維度幾乎無關。對之後換 backbone 的成本估算有用：換更大的 backbone 幾乎不影響 head 成本，換 relation 設計才會。

---

## 量測陷阱（四個教訓，值得並列）

**1. 4b：兩次完全相同的 run 本身就有噪聲，跟待測效應同量級時，比較毫無意義。**

RN18 B1，同 seed 重跑兩次（A1、A2），loss 曲線逐點差 mean 0.066，跟原本想驗證的 BN 修正範圍效應（B1 vs B2）落在同一量級。任何「跑兩次訓練比較」的實驗，做之前都要先測 A/A 同設定重跑的噪聲底線，不然看到「差不多」或「差一點」都無法下結論。細節見 `0918_階段4_等價性與BN修正.md`。

**2. 四段時間儀器漏了 H2D，驗收門檻過了不代表儀器完整。**

原本設計的四段（data / backbone / head / backward+step）在全微調情境下誤差 4–8%，低於 10% 的驗收門檻，看起來沒問題。但這個缺口其實一直都在——`prepare_task()` 的 `.to(self.device)` 從沒被計時過。全微調下 GPU 計算量大（300–700ms），這段缺口的絕對值（約 30ms）被稀釋到誤差門檻以下；凍結情境下 GPU 計算只剩約 50ms，同一段缺口把誤差撐到 44.35%，才暴露出來。**通過驗收門檻只代表「這次沒被抓到」，不代表「儀器涵蓋了所有段落」**——跟 4b 的教訓是同一類:噪聲/缺口是否可見，取決於待測訊號的量級，量級變了，同一個問題會重新出現。

**3. V4 等價性測試：`.eval()` 讓 checkpoint 分支從未被執行，測試在比較兩個做同一件事的模型。**

原本 4a 的 V4 用 `.eval()` 比較 grad_ckpt on/off 的 logits，報 `0.00e+00` 判 PASS。但 `forward()` 走 checkpoint 分支的判斷式是 `if self.training and grad_ckpt:`——`.eval()` 把 `self.training` 設成 `False`，判斷式恆為 `False`，**兩個模型其實走的是同一條 `else` 分支**，比出 `0.00e+00` 是必然的，跟 checkpoint 邏輯對不對完全無關。這個 bug 從 0918 埋到 0921 才在擴充測試時被發現，中間所有引用「等價性由 4a 結案」的地方都是錯的。**測試必須能證明它走過了它聲稱要測的路徑**——通過不代表測到；這條跟 4b（噪聲底線）、H2D（儀器漏段）是同一類教訓的第三種變體：4b 是「效應被雜訊蓋過」、H2D 是「儀器有沒量到的段落」、這條是「測試根本沒有執行到待測的程式碼分支」，三種都會讓「驗收通過」變成假訊號。修復與補測結果見 `0918_階段4_等價性與BN修正.md` 的「1b. 修好的 V4」。

**4. `test_iters` 有兩種獨立的觸發方式會讓某次評估被靜默跳過，規劃排程時都要避開。**

`run.py` 訓練迴圈裡的評估觸發條件是 `((iteration+1) in test_iters) and (iteration+1) != total_iterations`——這個 `!= total_iterations` guard 是既有行為，不是這次新增的。它有兩種獨立的踩雷方式：

- **`test_iters` 裡某個值剛好等於 `training_iterations`**：階段 7 soak 用 `--training_iterations 3000 --test_iters 1000 2000 3000` 跑的時候親自踩到——`3000` 那次評估從沒觸發，soak 原本設計 3 次評估，實際只跑了 2 次。真正的 100k 正式跑因此改用 `--training_iterations 100020`（config 裡本來就是這個值）搭配 `--test_iters 25000 50000 75000 100000`，讓 `100000 != 100020`，四次評估才都能真的觸發。
- **resume 時 `start_iteration` 剛好等於某個 `test_iter`**：如果訓練中斷後從 checkpoint resume，`start_iteration` 若剛好落在某個 `test_iter` 上，那次評估一樣會被這個 guard 排除（邏輯上是同一個 `!= total_iterations`／已經跑過該點的情境變體）。要用 `-m checkpointN.pt` 事後補跑那次評估，不能假設 resume 之後 `test_iters` 清單裡的每一項都真的會被跑到。

兩者是同一段 guard 邏輯的兩種觸發路徑，都要在規劃 `--test_iters` 清單、以及每次 resume 之後手動核對「這次 resume 有沒有跳過哪個 test_iter」。

---

## 正式 100k 執行結果（2026-09-20 18:15:58 → 2026-09-21 15:55:19）

`--training_iterations 100020`、`--seed 42`、`--test_iters 25000 50000 75000 100000`、`--num_test_tasks 10000`，`nohup` 背景跑，全程 `--profile_memory` 開著。

**跑完了，不是「跑得動」而是真的跑完了**：

- 總耗時 **21.66 小時**，跟階段 7 soak 外推的 21.22 小時只差 2%
- 全程 `retries` 都是 0，`cur_res` 從 9.705 GB 微升到 9.707 GB（+2MB，雜訊量級，不是階梯），`RUN PEAK alloc 7.664 GB / reserved 9.707 GB`——跟階段 1、soak 的數字幾乎完全一致
- 四次評估全部觸發（`test_iters` 最大值 100000 ≠ `training_iterations` 100020，避開了上面第 3 條陷阱），準確率穩定，沒有跳動或劣化：

  | iter | accuracy |
  |---|---|
  | 25000 | 49.98% |
  | 50000 | 50.36% |
  | 75000 | 50.34% |
  | 100000 | 49.75% |

- checkpoint：`~/trx_data/checkpoints/rn50_b4_flat8_s42_0920/hmdb_split3_20260920_181558/`，20 個 `checkpointN.pt`（iter 5000–100000，每 5000 一個）+ `checkpoint.pt` + `fully_trained.pt`，共 4.49 GB（不在 git 裡，路徑僅供之後取用）
- 執行 log：`logs/rn50_b4_flat8_s42_0920.log`

---

## 未量測 / 不成立的事

- ~~checkpointing 的數值等價性原本未驗證~~ **已於 0921 補測解決**：0918 寫的「等價性由 4a 的 V4 結案，位元級相同」是錯的結論——原 V4 在 `eval()` 下比較，`eval()` 讓 checkpoint 分支從未被執行，兩個模型其實走同一條路徑，`0.00e+00` 是兩個相同運算的比較，不是等價性證據。0921 修好 V4（加了分支旗標確認真的走了不同路徑），RN18 與 RN50 都補測，梯度差 = 控制組底線 = `0.000e+00`，這次是真的等價性證據。細節見 `0918_階段4_等價性與BN修正.md` 的「1b. 修好的 V4」。
- **RN50 沒有裸跑對照組（訓練管線層級的準確率對照，限制仍在）**：裸跑就 OOM，跑一整輪訓練比最終準確率沒辦法做；但單步梯度等價性（修好的 V4）不需要跑得動裸跑，CPU 上直接測了 RN50，這個層級的限制已經解除，結果同樣見「1b. 修好的 V4」。
- **最佳 `--ckpt_segments` 沒有量過**：全程只試過 `segments=8`（Colab 沿用下來的值），沒有掃過其他切法在本機的顯存/時間權衡，不知道 8 是不是本機的最佳點。
- **4b（loss 曲線逐點比較）已作廢**：同設定重跑的噪聲底線（mean 0.066）跟待測的 BN 修正範圍效應同量級，2000 iteration 尺度下沒有鑑別力。等價性改由修好的 V4 結案（見上）。

---

## 附錄

完整數據見 `logs/vram_0918_runpy.csv`（9 列，涵蓋階段 1、2、3、5、5b）。
BN momentum 修正範圍、hook 陷阱、4a/4b/4c 細節見 `0918_階段4_等價性與BN修正.md`。
