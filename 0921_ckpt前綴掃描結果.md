# ckpt 前綴掃描結果 — checkpointing 能不能換回時間

日期：2026-09-21
分支：`vram/ckpt-prefix-0921`
完整數據：`logs/ckpt_prefix_sweep_0921.csv`（8 列，另見 `verify_ckpt_shape.py` 的
1b/V4/V4b 結果，等價性驗證獨立記在 `0918_階段4_等價性與BN修正.md`）

## 一句話回答

**在這台 3080 Ti（12 GB）內，這次掃描找到的最快可行設定是 `--ckpt_prefix 11 --ckpt_segments 4`，715.150 ms/iter，比 flat8（`--ckpt_prefix 14 --ckpt_segments 7`，734.975 ms/iter）快 2.70%——低於 5% 的行動門檻，結論是「ckpt 切法在這張卡的顯存限制下不是有效的時間槓桿」，不改預設。**

---

## 顯存—時間取捨表

| N | segs | BN 涵蓋 | alloc GB | reserved GB | nvidia-smi GiB | ms/iter | backward+step ms | 可行？ |
|---|---|---|---|---|---|---|---|---|
| 4 | 2 | 1/53 | 未量測 | 未量測 | 11.499 | 未量測 | 未量測 | **在 segs=2 下不可行**（OOM，iter=1） |
| 7 | 4 | 11/53 | 未量測 | 未量測 | 0.851（輪詢沒抓到真峰值） | 未量測 | 未量測 | **在 segs=4 下不可行**（OOM，iter=1） |
| 11 | 6 | 24/53 | 8.234 | 11.182 | 11.630 | 712.159 | 472.670 | **在 segs=6 下不可行**（超過 11.0 GiB 門檻，沒 OOM 但太貼近） |
| 11 | 4 | 24/53 | 8.219 | 11.059 | **10.069** | **715.150** | 474.887 | ✅ **可行**（這次掃描的最快可行設定） |
| 11 | 3 | 24/53 | 未量測 | 未量測 | 11.606 | 未量測 | 未量測 | 在 segs=3 下不可行（OOM，iter=1） |
| 14 | 7 | 34/53 | 7.664 | 9.705 | 未量測（推估 ≈10.15，見下） | 734.975 | 496.227 | ✅ 可行（= flat8，現行預設） |
| 17 | 9 | 43/53 | 7.451 | 9.537 | 9.985 | 753.234 | 513.843 | ✅ 可行，餘裕大 |
| 20 | 10 | 53/53 | 7.265 | 9.434 | 9.884 | 771.564 | 532.441 | ✅ 可行，餘裕最大但最慢 |

N=14 這一列沒有另外起 nvidia-smi 輪詢（直接引用 1c 的結果），但 reserved
（9.705 GB）遠低於 11.0 GiB 門檻，可行性不受影響；用 N=17/N=20 的
reserved→nvidia-smi 差值（約 +0.45 GiB）推估 N=14 的 nvidia-smi 峰值約
10.15 GiB，這是推估不是實測，标记清楚。

---

## 階段 2 的假設有沒有成立

**成立，而且階段 3 的補測又驗證了一次。** 階段 2 在 N=14 上測 segs∈{2,7,14}，
time 差距 <0.7%；階段 3 補測 N=11 的 segs∈{3,4,6} 時，segs=6 跟 segs=4 兩個
可比的設定（712.159 vs 715.150 ms）只差 0.42%，一樣落在「時間只看涵蓋範圍，
不看段數」這條線內。**兩次獨立驗證都支持同一個結論。**

但顯存不是這樣——這件事被階段 2 的意外發現先預告了一次，階段 3 又用真實案例
證實了它會直接改變可行性判斷：N=11 在 segs=6 下 nvidia-smi 峰值 11.630 GiB
（超過門檻，不可行），换成 segs=4 就降到 10.069 GiB（可行），换成 segs=3
反而 OOM。**段數對顯存的影響不是單調、不能用公式預測，只能實測**——這正是
上一輪判讀時特別留的那條「補跑兩個段數」的伏筆，這次真的用上了：如果沒有
補測，N=11 會被誤判成「不可行」，而實際上它是這次找到的最快可行設定。

---

## 上限 22.4% 裡實際拿回了多少

理論上限是 backbone forward 佔整個 iteration 的 22.4%（163.5 / 729.4 ms，
階段 5 的量測，等於「完全不 checkpoint」能省下的全部時間——但 RN50 完全
不 checkpoint 在這張卡上直接 OOM，這個上限本來就摸不到）。

這次在 12GB 約束下，從 flat8（N=14）能推進到的最小可行 N 是 11，實際拿回：

```
(734.975 - 715.150) / 734.975 = 2.70%
2.70% / 22.4% ≈ 12.1%
```

**只拿回了理論上限的 12% 左右。** 大部分理論上可省的時間，在這張卡的顯存
限制下拿不到——要拿到更大的時間節省（N 降到 7 甚至更低），顯存直接爆掉
（N=7 在能找到的三種段數下全部 OOM，且是在第 1 個 iteration 就爆，不是
訓練途中才爆，代表連峰值都撐不住第一步）。2.70% 在實務上不值得為它切換
預設設定、增加設定複雜度、縮小顯存餘裕（flat8 的 margin 是 2.3 GiB 上下，
N=11/segs=4 只剩約 2.2 GiB，看起來差不多，但 flat8 是目前唯一有 21.66 小時
100k 完整驗證過的設定）。

---

## 階段 4（selective checkpointing）：跳過

按原計畫「若階段 3 的結論已經是『省不到多少』，這一段可以跳過」——階段 3
量到的最佳結果是 +2.70%，低於 5% 行動門檻，**跳過階段 4，不做 selective
checkpointing**。

---

## 結論：不改預設

`--ckpt_prefix` / `--ckpt_segments` 作為可選旋鈕留在 `run.py` 裡（已經是
`--ckpt_prefix` 未給時完全等同舊路徑的行為），但**預設維持 flat8
（`--ckpt_segments 8` 的 `checkpoint_sequential` 路徑，等同 `--ckpt_prefix 14
--ckpt_segments 7`）**。理由：

1. 這次掃描找到的最快可行設定只比 flat8 快 2.70%，低於行動門檻。
2. flat8 是唯一跑過 21.66 小時、四次評估全過的完整 100k 驗證過的設定；
   `--ckpt_prefix 11 --ckpt_segments 4` 只驗證過 300 iteration，顯存餘裕
   （約 2.2 GiB）比 flat8 更緊，換掉預設的風險不划算，換來的好處也不到 3%。
3. `--ckpt_prefix` 的等價性已經完整驗證（1b：三組配置，含刻意測 off-by-one
   的不整除案例；1c：在真實 GPU 上重現 flat8 的顯存與時間數字），這條路徑
   本身是可信的，只是這次沒找到值得換預設的理由。

---

## 附：本次涉及的檔案

- `run.py`：`--ckpt_prefix` flag、`_ckpt_forward_prefix()` 手動分組
  checkpoint 實作、BN 涵蓋範圍隨 `--ckpt_prefix` 調整的邏輯。
- `models/stage2_model.py`：`_ckpt_forward_prefix()`、`_split_even()`、
  `forward()` 裡的 `self._last_ckpt_branch` 旗標與 `ckpt_prefix` 分支路由。
- `verify_ckpt_shape.py`：V4/V4b（修好的等價性測試）+ 1b（三組 `--ckpt_prefix`
  等價性測試，含 off-by-one 案例）。
- `logs/ckptpfx_rn50_N14_s7.log`（=1c）、`logs/ckptpfx_rn50_N14_s{2,14}.log`
  （階段2）、`logs/ckptpfx_rn50_{3a,3b,3c-segs{3,4,6},3e,3f}.log`（階段3）、
  `logs/ckptpfx_rn50_3c-final.log`（N=11/segs=4 的完整 300 iter 量測）。
- `logs/ckpt_prefix_sweep_0921.csv`：本文件表格的完整數據來源。
