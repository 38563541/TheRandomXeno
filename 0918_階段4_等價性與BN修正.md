# 階段 4 — 等價性驗證與 BN momentum 修正範圍

日期：2026-09-19（跨到 2026-09-20 結案）
分支：`vram/rn50-runpy-0918`

## 結論先講（2026-09-21 更正 + 補測）

- ⚠️→✅ **0918 寫的「等價性由 4a 結案」是錯的結論，但 0921 補測之後，等價性
  現在是真的驗證過了。** 原 V4 在 `eval()` 下比較，`eval()` 讓
  `forward()` 裡 `if self.training and grad_ckpt:` 恆為 `False`，checkpoint
  分支從未被執行（`m_off`、`m_on` 走同一條 `else` 分支）；而且原本只比
  forward logits，測不到 backward 重算。**0.00e+00 當時是兩個做同一件事的
  模型互相比較，不是等價性證據。**
- **修好的 V4（0921）：RN18 與 RN50 都補測了，梯度差 = 0.000e+00，跟控制組
  底線（同模型同輸入跑兩次）完全等同，且用 `self._last_ckpt_branch` 旗標
  確認兩個模型真的走了不同分支。** 這次是真正的位元級等價性證據，細節見
  下方「1. 等價性驗證」與「1b. 修好的 V4」。
- **`bn_fix_scope` 預設改為 `covered`**（只修正真正被 `checkpoint_sequential`
  重算的 `chain[:n_ckpt]` 段），`all`（舊行為，修正到所有 BN）保留為可切換選項供對照。
- **4b（loss 曲線逐點比較）作廢**，理由見下。
- 發現兩個會反覆坑人的方法論陷阱：`register_forward_hook` 量不到
  non-reentrant checkpoint 的 backward 重算（見 3）；以及上面這個 V4
  的 `eval()` bug——兩者都是「驗收通過了，但儀器/測試根本沒走到它聲稱在測的路徑」。

**在等價性補測之前就已經成立、獨立於 V4 bug 的證據**（現在多了修好的 V4
這條直接證據，以下三件事仍然各自有效，可以互相印證）：
- **顯存數字本身就證明 checkpoint 分支在訓練時真的有執行**——階段 1/soak/
  100k 量到的 alloc 峰值（7.664 GB）遠低於裸跑會需要的量，這個省顯存的
  效果不可能來自沒有執行的程式碼路徑。
- **4c 的 BN 最小範例是獨立測試，真的跑了 backward**（見下方 3），直接量
  `running_mean` 而非透過 hook，證明 checkpoint 重算確實讓 BN 統計量更新
  兩次——這件事跟 V4 無關，不受這次發現的影響。
- **100k 正式跑完成，準確率跟七月沒開 ckpt 的 run 同量級**——這只能排除
  「嚴重損壞」，混淆變因太多，不能單獨當等價性證據，但現在有修好的 V4
  這條直接證據佐證，兩者方向一致。

---

## 1. 等價性驗證（4a）—— 0921 更正：原 V4 是空測試

`verify_ckpt_shape.py` V1–V4 當時全部回報 PASS：

| 項目 | 結果 |
|---|---|
| V1 state_dict 鍵名 | 125 keys，diff=0，PASS（仍然有效，跟 grad_ckpt 分支無關） |
| V2 載入既有 checkpoint | 0 unexpected keys，PASS（仍然有效，跟 grad_ckpt 分支無關） |
| V3 梯度流 | `resnet.0.weight.grad.abs().sum() = 2905858.0 > 0`，PASS（仍然有效——這個測試用 `.train()`，checkpoint 分支真的有跑，只是只確認梯度非零，沒有比對數值是否等價） |
| V4 數值等價（原版） | ~~max \|logits_off − logits_on\| = 0.00e+00 < 1e-4，PASS~~ **這個 PASS 是假的** |

**原 V4 的問題**：測試在比較前呼叫 `m_off.eval()` 與 `m_on.eval()`。
`forward()` 裡走 checkpoint 的判斷式是：

```python
if self.training and getattr(self.args, "grad_ckpt", False):
    ...(checkpoint_sequential 路徑)...
else:
    context_features = self.resnet(context_images).squeeze()
    ...
```

`.eval()` 會把 `self.training` 設成 `False`，這個判斷式因此恆為 `False`——
**`m_on`（原本設計要測 checkpoint 開啟的模型）實際上跟 `m_off` 走的是同一條
`else` 分支**，兩者運算完全相同，比出 `0.00e+00` 是必然的，跟 checkpoint
邏輯對不對完全無關。用最小範例直接驗證這個機制：

```python
m = CNN_TRX(make_args(grad_ckpt=True, ckpt_segments=8))
m.eval()
print(m.training)                                   # False
print(m.training and getattr(m.args, 'grad_ckpt'))  # False ← checkpoint 分支不會被觸發
```

另外就算判斷式為真，V4 原本也只比較 forward 出來的 logits（`with torch.no_grad()`
包住），checkpoint 真正要驗證的「backward 重算出來的梯度是否跟正常 backward
一樣」從來沒被測過——forward-only 的比較本來就測不到這件事。

**修好後的 V4（0921）**：見下方新增小節「1b. 修好的 V4」，結果另外記錄。

---

## 1b. 修好的 V4（0921）

修法：兩個模型都維持 `.train()`（checkpoint 分支才會被觸發）；所有 BN
的 `momentum` 設成 0（running stats 不更新，避免污染比較，但不影響 train
模式下這次 forward 的輸出，跟原本用 `.eval()` 的目的一樣但不會連帶關掉
checkpoint 分支）；所有 dropout 的 `p` 設成 0（`train()` 模式下 dropout
會讓兩個模型隨機不同，這是原 V4 沒處理到的另一個潛在污染源）；`m_on`
的權重從 `m_off` 複製，同一份輸入；拿掉 `no_grad()`，做一次
`loss.backward()`，比較每一個參數的 `.grad`；在 `forward()` 裡加了一個
分支旗標，測試裡 `assert` 走過的分支跟預期一致（這是這次最重要的一行——
沒有這行，V4 的 bug 就是這樣被漏掉的）；控制組是 `m_off` 用同一份輸入跑
兩次，算出的差值當底線；全部在 CPU 上跑，求位元級可比，輸入沿用原本
V4 的 84×84 小圖（RN50 不開 ckpt 在這個尺寸下 CPU 也跑得動，不需要 GPU）。

**結果（RN18 與 RN50 都測了，舊 flat8 路徑 `--grad_ckpt --ckpt_segments 8`，不用新的 `--ckpt_prefix`）：**

| backbone | branch_off | branch_on | 控制組底線 \|off−off2\| | \|grad_off−grad_on\| 全體參數 max | 同一位置 backbone 第一層 diff |
|---|---|---|---|---|---|
| RN18 | `none` | `checkpoint_sequential` | 0.000e+00 | **0.000e+00**（在 `resnet.0.weight`） | 0.000e+00 |
| RN50 | `none` | `checkpoint_sequential` | 0.000e+00 | **0.000e+00**（在 `resnet.0.weight`） | 0.000e+00 |

`self._last_ckpt_branch` 旗標確認 `m_off` 真的走了 `none`（不開 ckpt 的一般路徑）、
`m_on` 真的走了 `checkpoint_sequential`——這次不再是「兩個模型跑同一段程式碼」。
控制組（`m_off` 同輸入跑兩次）底線是 `0.000e+00`，跟 ckpt 開/關的梯度差
（同樣 `0.000e+00`）完全等同，判讀：**ckpt 梯度差 ≤ 控制組底線，等價性通過**。

**這才是第一次真正證明舊路徑（`checkpoint_sequential`，即 flat8）在數值上等價**，
RN18 與 RN50 都驗證過，不再只是「推論可以推及 RN50」——是實測。CPU 上跑，
84×84 小圖，不需要 GPU 也不需要裸跑對照組，因為驗證的是單步梯度，不是
完整訓練管線跑不跑得動。

執行方式：`python verify_ckpt_shape.py`，log 見 commit。

---

## 2. 4b（loss 曲線逐點比較）作廢：為什麼

原設計：RN18 B1，同一個 `--seed 42`，2000 iteration，四條 run（A1/A2 兩次
ckpt-off 對照、B1 `bn_fix_scope=all`、B2 `bn_fix_scope=covered`），比較逐點
loss 差，想看「B1 明顯比 B2 差」來證明 `all` 是 bug。

**A1、A2 是完全相同的指令重跑兩次**（同 seed、ckpt 都關）。結果：

| 比較 | max | mean |
|---|---|---|
| \|A2 − A1\|（同設定重跑，cuDNN 不確定性的噪聲底線） | 0.771756 | 0.066148 |
| \|B2 − A1\|（covered 修法） | 0.836097 | 0.079279 |
| \|B1 − A1\|（all，現況） | 0.374927 | 0.058052 |

三組差值全部落在同一個量級。`B1 < B2`（現況修法反而比較接近 A1）不代表
`all` 修法比較對——這只是同分布抽樣的順序，是噪聲，不是訊號。

**根因**：兩次完全相同的 run（A1 vs A2）本身就有 mean 0.066 的 loss 差異，
這是 cuDNN 的非確定性（沒開 `cudnn.deterministic`）加上 SGD 的非線性動態，
把任何微小的浮點差異在 2000 iteration 內放大到這個量級。待測的效應
（BN momentum 修正範圍對不對）如果真實存在，也只會在早期造成極小的數值
差異，會被同一套放大機制蓋過，跟「什麼都沒變、純噪聲」的量級無法區分。

**這條結論不只適用於這次**：任何用「跑兩次訓練、比較 loss 曲線」來驗證
某個數值修正是否正確的實驗，都必須先測 A/A 同設定重跑的噪聲底線，
再判斷待測效應是否明顯超出底線。如果沒有這個底線對照組，看到兩條曲線
「差不多」或「差一點」都無法下結論——差多少算差，本身就需要一個噪聲量級
作為參照系。這次是運氣好先測了底線，才沒有把噪聲誤讀成訊號。

loss csv 檔案（`logs/loss_A1_0918.csv` 等四份）保留供之後回頭查，
但不再作為等價性證據使用——等價性改用修好的 V4（見下方「1b. 修好的 V4」）。

---

## 3. `register_forward_hook` 量不到 checkpoint 的 backward 重算

原本設計的 4c 想直接數「每個 BN 一次 iteration 被呼叫幾次」來驗證
`bn_fix_scope=covered` 對、`all` 錯，用 `register_forward_hook` 計數。

結果：不管 BN 在不在 `chain[:n_ckpt]` 涵蓋段內，hook 計數**全部都是 2**
（來自 support/query 各跑一次 backbone，跟 checkpoint 重算完全無關）。
一開始看起來像是「BN 修正根本沒有作用」，但沒有直接下這個結論，
先去查證計數方法本身可不可信。

用一個獨立最小範例直接量 `running_mean`（不透過 hook）：

```python
# 手動呼叫兩次 BN（模擬「真的更新兩次」）
manual 2x forward   running_mean = [-0.0055, 0.0081, 0.0024, -0.0112]

# 只呼叫一次
manual 1x forward   running_mean = [-0.0029, 0.0042, 0.0013, -0.0059]

# checkpoint(fn, x) 一次 + backward() 一次
checkpoint 版本      running_mean = [-0.0055, 0.0081, 0.0024, -0.0112]
```

`checkpoint()` 版本的 `running_mean` 跟「手動兩次」完全相同，不是「一次」。
再對同一個範例掛 `register_forward_hook`：

```
forward 剛跑完（backward 前）   hook count = 1
backward()（recompute 應該發生）hook count = 1   ← 沒有變成 2
running_mean 仍然是「更新兩次」的數值
```

**結論：PyTorch 的 non-reentrant checkpoint（`use_reentrant=False`）在
backward 重算時不會觸發 `register_forward_hook`，但底層的 forward 運算
（包括 BN 統計量更新這類副作用）確實有重新執行一次。** 這代表：

- 原本 BN momentum 修正的前提（checkpoint 重算讓涵蓋段的 BN 統計量真的
  更新兩次）是對的，這件事已經獨立驗證過，不是憑空假設。
- 但「用 hook 數呼叫次數」這個驗證手法本身是失效的儀器——任何以後想用
  hook 去觀察 checkpoint 涵蓋段行為的嘗試，都會得到跟這次一樣的假陰性。
  要驗證要嘛直接比 `running_mean`／`running_var` 這類有副作用的狀態量，
  嘗嘛用其他不依賴 hook 的機制。

（PyTorch 版本：2.5.1+cu121。這個行為沒有在官方文件裡明確保證，之後升級
PyTorch 版本時應該重新確認一次。）

---

## 4. 限制（0921 更新：RN50 梯度等價性限制已解除，訓練管線層級的限制仍在）

RN50 裸跑就 OOM（見階段 1 之前的量測），沒有 no-ckpt 對照組可比——
**完整訓練管線層級的準確率對照**（跑一整輪訓練，比最終準確率）在 RN50 上
仍然沒辦法直接做，這個限制沒有解除，因為它本質上需要一個能同時裝下
裸跑與 ckpt 兩種設定的 GPU 才能對照，而裸跑本身就 OOM。

但 **V4 這類「同一份輸入比梯度」的數值等價測試不需要跑得動完整訓練管線，
只要塞得下一次 forward+backward 就夠**——0921 修好 V4 之後，RN50 也直接測
了（CPU 上、84×84 小圖，不需要 GPU），結果見上方「1b. 修好的 V4」：梯度差
0.000e+00，跟控制組底線相同。**這個層級（checkpoint 重算本身在數學上等不
等價於不重算）的限制已經解除，RN50 是實測過的，不再是推論。**

支持「這個結論可以類推到完整訓練管線」的機制性理由（0918 的原始思路，
現在有 1b 的實測背書，不再只是推論）：

- `checkpoint_sequential` 的數學性質（重算同一段前向以省顯存）跟 backbone
  是 RN18 還是 RN50 無關，是 PyTorch checkpoint 機制本身的性質。
- `_ckpt_chain()` 的攤平邏輯、BN momentum 的修正邏輯（`bn_fix_scope=covered`）
  對兩個 backbone 用的是同一段程式碼路徑，只是 chain 長度、BN 位置不同
  （RN18 涵蓋 8/20，RN50 涵蓋 34/53——見階段 2 的稽核）。

---

## 附：本階段涉及的檔案

- `run.py`：`--bn_fix_scope {all,covered}`（預設 `covered`），`init_model()`
  裡的 BN momentum 修正段落補了本文件摘要的註解。
- `logs/loss_{A1,A2,B1,B2}_0918.csv`：4b 的逐 iteration loss，保留但不再
  作為等價性證據。
- `logs/stage4b_{A1,A2,B1,B2}_0918.log`：對應的訓練 log。
- `verify_ckpt_shape.py`（0921 修好 V4）：`models/stage1_model.py` 與
  `models/stage2_model.py` 的 `forward()` 新增 `self._last_ckpt_branch`
  旗標，供測試確認實際走了哪條分支；`make_args()` 加了 `method` /
  `trans_linear_in_dim` / `ckpt_prefix` 參數，RN18/RN50 都能測。
