# 0922 — checkpoint 方法比較（無人值守版）結果

日期：2026-09-21 23:44 → 2026-09-22 00:42（約 1 小時，遠低於 16 小時預算）
分支：`vram/ckpt-methods-0922`

## 給使用者看的摘要

- **六個階段全部跑完**（0、1、2、3、4、5），沒有觸發 🛑 全停。
- 唯一的失敗類別是 ⏭️ 跳過並繼續：C1/C2/C3（selective checkpointing 的
  時間/顯存量測）全部 OOM，已照實記錄，不影響其他階段。
- **最大發現**：AMP（bf16 或 fp16）＋現行 flat8 checkpoint 疊加，比
  純 flat8 快 **30–35%**，遠遠超過 0921 定的 5% 行動門檻，顯存餘裕
  還比 flat8 更大。**但本次規則明確不准改任何預設值**，這個結果原封
  不動留給你決定要不要用。
- Selective checkpointing（`conv` 全存、其他重算）在這個 ResNet-Bottleneck
  架構上是反效果——比不開 SAC 的 flat8 還吃顯存，連涵蓋整個 backbone
  都救不回來，三組全部 OOM。

---

## 總表

| tag | 設定 | status | alloc GB | reserved GB | nvidia-smi GiB | ms/iter | vs R0 | 可行(≤11.0GiB) |
|---|---|---|---|---|---|---|---|---|
| R0 | flat8（N14/s7,full,fp32） | ok | 7.664 | 9.705 | 10.156 | 735.313 | 基準 | ✅ |
| S2 | N11/s4,full,fp32 | ok | 8.219 | 11.059 | 11.070 | 708.431 | -3.66% | ❌(超0.07) |
| C1 | N14/s7,**save_conv**,fp32 | **oom** | 未量測 | 未量測 | 11.701 | 未量測 | — | ❌ OOM |
| C2 | N11/s4,**save_conv**,fp32 | **oom** | 未量測 | 未量測 | 11.475 | 未量測 | — | ❌ OOM |
| C3 | N21/s10（全backbone）,**save_conv**,fp32 | **oom** | 未量測 | 未量測 | 11.633 | 未量測 | — | ❌ OOM |
| A1 | 不開ckpt,**bf16** | ok | 10.610 | 11.273 | **11.723** | 430.066 | **-41.52%** | ❌(超0.723) |
| A2 | N14/s7,full,**bf16** | ok | 4.220 | 5.965 | 6.414 | 515.287 | **-29.92%** | ✅ |
| A3 | N11/s4,full,**bf16** | ok | 4.583 | 6.385 | 6.834 | 499.629 | **-32.06%** | ✅ |
| A4 | N14/s7,full,**fp16** | ok | 4.222 | 5.812 | 6.262 | 474.307 | **-35.49%** | ✅ |

（S3/S4/A5/A6 是 RN18/RN34 參考點，跟本任務主題 RN50+B4 的時間比較不適用同一張表，見下方顯存拆分表。）

---

## 顯存拆分表（S1–S4，`persist / bb / head / bwd_extra / peak`，單位 GB）

| tag | backbone | persist | bb_retained | head_retained | bwd_extra | peak(alloc) |
|---|---|---|---|---|---|---|
| S1(=R0) | RN50 N14 | 0.545 | 5.349 | 0.139 | 1.629 | 7.664 |
| S2 | RN50 N11 | 0.546 | 6.877 | 0.142 | 0.654 | 8.219 |
| S3 | RN18 裸跑 | 0.427 | 4.841 | 0.132 | 0.128 | 5.529 |
| S4 | RN34 N14 | 0.506 | 2.179 | 0.131 | 0.535 | 3.350 |

判讀：`head_retained` 在四組之間幾乎不變（0.131–0.142 GB），跟 backbone
特徵維度（RN18/RN34 512維 vs RN50 2048維）無關——呼應 0918 發現的
「head 時間不隨特徵維度變」，這次確認顯存也是這樣。`bwd_extra` 在開
ckpt 的三組（S1/S2/S4）明顯偏大，沒開 ckpt 的 S3 很小——peak 出現在
backward 重算段，不是 backward 剛開始。

---

## 四句話回答

**SAC 比 flat8 快多少？**
**未量測**，原因：C1/C2/C3（涵蓋範圍分別對應 flat8、更小的 N、以及全
backbone）全部 OOM，連涵蓋整個 backbone 的 C3（nvidia-smi 11.633 GiB）
都撐不住。3b/3c 已經證明 SAC 的實作在數值上正確（梯度等價、BN
running_mean 跟 full 完全相同），這是純粹的顯存行為，不是 bug：
`policy_fn` 對每個 conv 都回傳 `MUST_SAVE`，ResNet Bottleneck 每個
block 有 3 個 conv，全部存下來的量比「segment 只存邊界、整段重算」的
full 策略還吃記憶體。

**AMP bf16 ＋ ckpt 比 flat8 快多少？**
A2（bf16, N14/s7，跟 flat8 涵蓋範圍相同）快 **29.92%**；縮小涵蓋範圍到
N11/s4（A3）快 32.06%；fp16 版本（A4）最快，35.49%，但 fp16 用
`GradScaler`，訓練穩定性/準確率風險比 bf16 高一些（4c 的梯度偏差量化
顯示 fp16 在這次量測的相對誤差反而比 bf16 小，但那只是單步梯度的
描述性數字，不是穩定性結論）。三個 AMP+ckpt 設定顯存餘裕都遠超
11.0 GiB 門檻（6.2–6.8 GiB），比 flat8 本身（9.7 GiB reserved）還寬鬆。

**RN50 不開 ckpt、只開 bf16，塞不塞得下？**
**塞得下，但沒有安全餘裕。** A1：nvidia-smi 峰值 11.723 GiB，不 OOM，
但超過本專案一貫用的 11.0 GiB 安全門檻——12GB 卡上只剩不到 300MB
自由空間，一有外部干擾（見 0920 soak 任務提過的margin風險）就可能
OOM。時間上最快（430.066ms/iter，比 flat8 快 41.52%），如果使用者
願意接受更緊的安全邊際，這是唯一「完全不用 checkpoint」的可行選項。

**RN50 下匹配頭留存的顯存大約是多少？**
約 **0.14–0.19 GB**（`head_retained` 欄位：R0=0.139、S2=0.142、
A1=0.193、A2=0.181、A3=0.189、A4=0.182），不到 200MB，而且幾乎不受
ckpt 涵蓋範圍或 AMP 開關影響——匹配頭本身的顯存需求很小，跟 backbone
比起來是零頭。

---

## 明確寫出這次沒有驗證的

- **AMP 的準確率等價：未驗證。** 4c 的梯度偏差量化（bf16/fp16 相對
  fp32 的 `‖g−g_fp32‖/‖g_fp32‖` 與 cosine similarity）只是量化單步
  梯度偏差大小，backbone 前段偏差明顯（bf16 相對誤差 47%、cosine
  0.888），head 段很小（bf16 相對誤差 1.9%、cosine 0.9998）——這不能
  換算成「準確率會差多少」，只是給之後設計準確率驗證一個參照基準。
- **任何新設定的長時間穩定性：未驗證（只測了 A4 一個設定的 3000
  iteration 趨勢）。** A4（fp16+ckpt N14/s7）的 alloc/reserved/cur_res
  在 3000 iteration 裡完全零漂移，`retries` 全程 0，loss/accuracy 正常
  收斂——但這不能取代 100k 的 soak，只是看有沒有明顯漂移。A1/A2/A3、
  SAC（若未來調整 policy 後可行）都沒有做過同等的趨勢檢查。
- **SAC 下的 BN 更新次數：已驗證，結果是「相同」。** 3c 直接比
  `running_mean`（BN momentum 保持預設 0.1，不歸零），SAC 涵蓋段內
  8 個 BN 的 `running_mean` 跟 full 完全相同（位元級）——SAC 不改變
  BN 被呼叫的次數，現行 momentum 修正邏輯照樣適用，不需要額外處理。

---

## 跳過或失敗的項目清單

| 項目 | 狀態 | 原因 |
|---|---|---|
| C1（SAC, N14/s7） | oom | nvidia-smi 11.701 GiB，"Tried to allocate 308 MiB...11.57GiB in use" |
| C2（SAC, N11/s4） | oom | nvidia-smi 11.475 GiB，同類 OOM |
| C3（SAC, N21/s10，全backbone） | oom | nvidia-smi 11.633 GiB，連全 backbone 涵蓋都救不回來 |
| 階段0第5項（SAC API可用性檢查） | 未觸發跳過 | `create_selective_checkpoint_contexts` 存在（True），3a/3b/3c/3d 照常執行 |
| bf16 支援檢查 | 未觸發跳過 | `torch.cuda.is_bf16_supported()` = True，A1/A2/A3/A6 照常執行 |
| GPU忙碌跳過(gpu_busy) | 沒有發生 | 全程沒有遇到 GPU 被佔用需要跳過的情況 |
| timeout | 沒有發生 | 所有設定都在 25 分鐘內結束（OOM 的三組在 7 秒內就結束） |
| NaN/Inf(status=nan) | 沒有發生 | 包括 fp16（A4）在內，300/3000 iteration 全程 loss 有限 |

---

## 建議追加（只列，不跑）

- **A1（bf16，完全不開 ckpt）的長時間穩定性**：這次階段5只選了 A4
  做3000-iter趨勢，A1因為超過11.0GiB門檻沒被選為候選；但A1是唯一
  「完全不用checkpoint」的可行選項、時間最快，如果使用者願意接受
  更緊的顯存邊際（11.723 GiB / 12 GB），值得專門補測它的3000-iter
  甚至100k穩定性。
- **換一個 SAC policy_fn 再試一次**：這次的 policy 是「所有 conv
  MUST_SAVE、其他 PREFER_RECOMPUTE」，結果比不開SAC還吃顯存。可以試
  「只有每個block的最後一個conv MUST_SAVE」或其他更保守的policy，
  看能不能讓SAC真正達到省顯存的效果，而不是變成反效果。
- **AMP 的準確率等價驗證**：完整訓練跑到收斂，比較 bf16/fp16 跟 fp32
  最終準確率的差異；或者比4c更進一步，統計多個不同batch的梯度cosine
  similarity分布，而不是單一batch的單點量測。
- **A1/A2/A3 其中之一的 100k 完整 soak**：這次只做了A4一個設定的
  3000-iter趨勢，沒有做100k等級的長時間驗證。
- **AMP + SAC 疊加**：這次AMP的四組（A1-A4）都用ckpt_policy=full，
  沒有測AMP+SAC組合——鑑於SAC本身已經在fp32下OOM，這個組合大概率
  也會OOM，這次沒有浪費時間去跑，但如果之後SAC的policy_fn調整到
  真正省顯存，這個組合值得補測。

---

## 附：本階段涉及的檔案

- `run.py`：`--ckpt_policy {full,save_conv}`、`--amp {off,bf16,fp16}`、
  顯存拆分（m_pre/m_bb/m_fwd/m_peak，每 iteration reset）、ms/iter
  中位數排除前20個暖機iteration、NaN/Inf 檢測（`sys.exit(97)`）。
- `models/stage2_model.py`：`_ckpt_forward_prefix()` 加 `policy` 參數
  （SAC 用 `create_selective_checkpoint_contexts` + `policy_fn`），
  `self._last_ckpt_branch` 改成 `prefix_full`／`prefix_save_conv`
  （原名 `ckpt_prefix` 已改名），`forward()` 加 `self._mem_bb` 顯存
  拆分點。
- `scripts/run_ckpt_methods_0922.py`：driver，13 個設定的清單、獨立
  process + 25分鐘timeout + nvidia-smi輪詢、log解析寫CSV、狀態分類、
  GPU閒置等待。
- `verify_ckpt_shape.py`：`run_ckpt_prefix_equivalence()` 加
  `ckpt_policy` 參數，`make_args_s2()` 加 `ckpt_policy`；1b 既有測試
  因分支改名同步更新期望值。
- `verify_amp_grad.py`（新增）：4c 的 AMP 梯度偏差量化腳本。
- `logs/ckpt_methods_0922.csv`：總表完整數據來源（13列）。
- `logs/m0922_*.log`、`logs/m0922_*_nvsmi.log`：每個設定的完整輸出與
  nvidia-smi 輪詢原始記錄。
- `logs/verify_ckpt_shape_0922_3b.log`：3b 等價性測試完整輸出（12項全過）。
- `logs/verify_sac_bn_0922.log`：3c BN running_mean 比較完整輸出。
- `logs/verify_amp_grad_0922.log`：4c 梯度偏差量化完整輸出。
- `logs/m0922_stage5_A4trend.log`：階段5 A4 的 3000-iteration 趨勢記錄。
