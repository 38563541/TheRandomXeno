# 0922b — fp16 AMP 準確率初篩結果

**狀態：進行中，階段 2（fp16 100k）已完成 2 / 4 評估點**

分支：`vram/amp-acc-0922`

PID 1927817，`nohup` 背景跑，啟動於 2026-09-22（見 `logs/rn50_amp_fp16_100k.log`）。

---

## 判定結果

（階段 2/3 完成後才能填）

## 配對表

| 檢查點 | fp32 | fp16 | fp16 − fp32 |
|---|---|---|---|
| 25k | 49.984%（CI 0.4249） | **50.724%**（CI 0.4251） | **+0.740 pp** |
| 50k | 50.362%（CI 0.4284） | **50.700%**（CI 0.4271） | **+0.338 pp** |
| 75k | 50.338% | 未量測 | 未量測 |
| 100k | 49.752% | 未量測 | 未量測 |
| **平均** | 50.109% | 未量測 | 未量測 |

25k、50k 評估前後 `[mem]` 都完全一致（`win_alloc 4.223 win_res 5.814
cur_res 5.814 retries 0`），沒有洩漏。訓練在 50000 評估完成後正常繼續
（iter 51000 已出現）。

## 階段 1 一句話結論

fp16 的梯度偏差（cosine similarity 0.98–0.997）遠比換一個訓練 episode 造成的
梯度差異（cosine similarity 0.0007–0.07，幾乎不相關）小一個數量級以上——見
下方階段 1 完整結果。

## 總耗時

（階段 2 完成後才能填）

---

## 階段 0 — fp32 對照組

### 1. GPU 閒置檢查

`nvidia-smi`：132 MiB，閒置，繼續。

### 2. 分支

```
git checkout stage2-decouple && git pull
git checkout -b vram/amp-acc-0922
```
HEAD = `4e5ae8c`（`0922_ckpt方法比較結果.md` merge 後的 `stage2-decouple`）。

### 3. fp32 RN50 100k 對照組

**完整指令**（來自 `logs/rn50_b4_flat8_s42_0920.log` 第 2 行 `Options: Namespace(...)`）：

```
python run.py --dataset hmdb --split 3 \
  --config configs/stage2_true_hyrsm_b4_tuple.yaml \
  --method resnet50 \
  --grad_ckpt --ckpt_segments 8 \
  --seed 42 \
  --test_iters 25000 50000 75000 100000 \
  --num_test_tasks 10000 \
  --print_freq 1000 --save_freq 5000 \
  --profile_memory \
  -c ~/trx_data/checkpoints/rn50_b4_flat8_s42_0920 \
  --scratch ~/Documents/work/trx/trx_data
```

⚠️ **注意**：這次跑的是 `--grad_ckpt --ckpt_segments 8`（舊的
`checkpoint_sequential` 路徑），**不是** 0921/0922 之後用的
`--ckpt_prefix 14 --ckpt_segments 7`。0921 階段 1c 已經用 V4 梯度等價
測試證明這兩條路徑在數值上完全等價（`0918_階段4_等價性與BN修正.md`），
所以這不影響本次比較的有效性——但階段 2 的 fp16 run **必須原封不動沿用
這次的 `--grad_ckpt --ckpt_segments 8`**，不能換成 `--ckpt_prefix`
語法，否則就不是「只改兩處」了。

- seed = 42
- config = `configs/stage2_true_hyrsm_b4_tuple.yaml`
- commit = `bf34a2a`（run.py launch 當下的 HEAD；一分鐘後的 `2f4c0ad`
  只改了 markdown，不影響執行中的程式碼）
- checkpoint 目錄 = `~/trx_data/checkpoints/rn50_b4_flat8_s42_0920/hmdb_split3_20260920_181558/`

**四個評估點**（來自 log 第 298/581/864/1147 行）：

| iter | accuracy | confidence(CI) |
|---|---|---|
| 25000 | 49.98400117620826 | 0.4248816027926246 |
| 50000 | 50.362001181393865 | 0.4283884683916564 |
| 75000 | 50.33800119444728 | 0.4224360563935274 |
| 100000 | 49.75200118303299 | 0.42452827041028707 |

**兩邊對得上才算數**——原本規則要求跟 `results.csv` 對，但這次跑
`--profile_memory` 時，`run.py` 的既有 guard（0921 patch五）會
**跳過 results.csv 寫入**（log 第 303/586/869/1152 行都印
`[mem] profiling run — 跳過 results.csv 寫入`），所以 `results.csv`
裡完全沒有這次的紀錄，不是矛盾，是設計如此。改用第二個獨立來源比對：
`0918_RN50上機驗證結果.md` 裡「正式100k執行結果」一節記錄的準確率
（49.98%/50.36%/50.34%/49.75%，四捨五入到小數點後兩位）跟 log 原始
數字完全吻合，兩個獨立寫下的來源一致，視為可靠。

### 4. 評估 episode 是否固定

**結論：沒有獨立固定 seed，但透過 Python 全域 `random` 模組的狀態，
事實上是固定的（同 `--seed` 兩次 run 應該評在同一批 episode 上）。**

依據（程式行號）：

1. `video_reader.py` 的 `VideoDataset.__getitem__`（第317行起）用
   `random.sample`（第322、340行）、`random.shuffle`（第353、357行）
   等取樣 episode，這些都是 Python **全域** `random` 模組的呼叫
   （`video_reader.py` 第8行 `import random`，沒有另外建立
   `random.Random()` 實例）。
2. `run.py` 的 `--seed` 在 `Learner.__init__` 一開始就呼叫
   `random.seed(_s)` / `np.random.seed(_s)`（第131-132行），**先於**
   `self.vd = video_reader.VideoDataset(self.args)`（第150行）的建立。
3. **訓練**用的 `self.video_loader` 是 `num_workers=10`
   （第162-164行），DataLoader 會 fork 出 10 個獨立的 worker
   **子行程**，每個 worker 用 `worker_init_fn`（第152-159行）各自
   重新 seed（`base*1000+worker_id`）——這些 worker 消耗的是「自己
   那份」隨機狀態，**不會**動到主行程的全域 `random` 狀態。
4. **評估**用的 `self.test_loader` 是 `num_workers=0`（第167行），
   直接在**主行程**裡跑，所以 `test()`（第843行起）呼叫
   `__getitem__` 時，消耗的是主行程的全域 `random` 狀態——而這個狀態
   從 `random.seed(42)` 之後，到第一次 `test()` 呼叫之前，沒有被
   其他任何程式碼動過（`run.py` 裡唯一另一處 `np.random.*` 呼叫是
   `shuffle()` 方法第892行的 `np.random.permutation`，但這個方法
   在整個 `run.py` 裡**從未被呼叫**，是死碼）。

**因此**：兩個 `--seed 42` 的 run，只要 `--num_test_tasks` 一致
（這次都是10000），四個評估點理論上會依序評在完全相同的一批 episode
上——這是根據程式碼分析得出的結論，**沒有另外用兩次真實 run 的輸出
去逐 episode 比對驗證**，如果階段 3 配對比較出現無法解釋的大幅波動，
這條推論是第一個要回頭檢查的地方。

**全停條件檢查**：對照組找到了，數字沒有互相矛盾的版本，**不觸發全停**。

---

## 階段 1 — 重做梯度偏差量測

### 4c 用的 loss 到底是什麼

`verify_amp_grad.py`（0922 舊版）第74、80行：`loss = out.sum()`——
`out` 是 `model(...)["logits"]` 的原始輸出，直接加總，**不是**分類
loss。這解釋了 0922 報告裡 `loss: fp32_a=-7158.497070` 這種負值——
交叉熵不可能是負的，logits 加總可以是任何值。

真正訓練用的 loss 是 `utils.py` 第136-148行的 `loss()` 函式（`run.py`
的 `train_task()` 呼叫 `self.loss(target_logits, target_labels,
self.device) / self.args.tasks_per_batch`）——這次重做全部改用這個。

### 重做結果（`verify_amp_grad_v2.py`，GPU，RN50，N14/segs7，
BN momentum=0，dropout=0，同一組初始權重）

```
loss（真正的分類 loss，utils.loss()）：
  fp32(ep1)重跑1 = 4.333467
  fp32(ep1)重跑2 = 4.333467
  fp32(ep2)      = 5.967283
  bf16(ep1)      = 4.282959
  fp16(ep1)      = 4.334625

|loss_fp32(ep1)重跑1 - 重跑2| = 0.000000e+00  (GPU 底線)
|loss_fp32(ep1) - loss_fp32(ep2)| = 1.633816e+00  (換episode)
|loss_bf16(ep1) - loss_fp32(ep1)| = 5.050850e-02  (AMP bf16偏差)
|loss_fp16(ep1) - loss_fp32(ep1)| = 1.157761e-03  (AMP fp16偏差)
```

**梯度偏差（三個探測點）：**

| 探測點 | GPU底線 rel/cos | 換episode rel/cos | fp16偏差 rel/cos | bf16偏差 rel/cos |
|---|---|---|---|---|
| backbone第一層(resnet.0.weight) | 0.0000e+00 / 1.00000000 | 1.9244e+00 / 0.00256497 | 1.9568e-01 / 0.98080850 | 5.2262e-01 / 0.86519849 |
| backbone最後block(resnet.7.0.conv1.weight) | 0.0000e+00 / 1.00000012 | 1.9602e+00 / 0.00075421 | 1.5905e-01 / 0.98733497 | 4.3337e-01 / 0.90743256 |
| head第一層(embed_linear.weight) | 0.0000e+00 / 1.00000012 | 1.9018e+00 / 0.07198469 | 7.9803e-02 / 0.99681145 | 2.3426e-01 / 0.97258806 |

### 判讀

**fp16 的偏差比「換一個 episode」的雜訊小一個數量級以上，而且方向
幾乎不受影響：**

- 相對誤差量級：fp16 是 0.08–0.20，換 episode 是 1.9–2.0——差了
  一個數量級。
- 方向一致性（cosine）：fp16 是 0.98–0.997（幾乎同方向），換
  episode 是 0.0007–0.07（幾乎不相關/正交）——這是質的差異，不只是
  量的差異。SGD 本來就要在訓練過程中吸收「每個 episode 梯度方向都
  不一樣」這件事，fp16 引入的方向偏移遠小於這個訓練本來就要吸收的
  變異量。
- bf16 的偏差比 fp16 大（相對誤差 0.23–0.52，cosine 0.87–0.97），
  但仍然比換 episode 的雜訊小、方向更一致。

**這個結果支持先驗證 fp16**（使用者原本的問題）：fp16 的梯度偏差
在這次量測裡全面優於 bf16（誤差更小、cosine 更高），跟 0922 4c
當時（用錯誤的 loss）觀察到的方向一致——那次的結論在方向上是對的，
只是量出來的具體數字因為 loss 選錯而不能用。

**這不是準確率驗證**——只是單步梯度層級的描述性比較，真正的答案要
看階段 2/3 的 100k 配對比較。

執行方式：`python verify_amp_grad_v2.py`，完整輸出見
`logs/verify_amp_grad_v2_0922b.log`。
